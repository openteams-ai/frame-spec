# v0.2 Complete Example

This example shows a fuller `v0.2` Frame shape.

It includes:

- the required fields
- suggested fields such as `version`, `scope`, `maintainer`, and `inherits`
- a concrete inheritance example

Use this example when you want a more realistic starting point for shared or evolving Frames.
